/*
1. Loop nvariable
2. Condition inside while 
3. Loop bady
4. Change the loop variable
*/ 


var roastGive = 0;
while (roastGive < 7) {
    console.log('roast Dan, Please !!  ')
    // roastGive = roastGive + 1;

    // Shorthand
    // roastGive += 1;
    console.log(roastGive);
    roastGive++;
}